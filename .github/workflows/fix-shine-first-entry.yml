# IF YOU HAVE NOT ALREADY READ PATIENT_CARE_RULES.md, STOP AND READ IT BEFORE EDITING THIS FILE.
name: Fix SHINE first entry
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/fix-shine-first-entry.yml
      - .github/fix-shine-first-entry.py
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - run: python3 .github/fix-shine-first-entry.py
      - run: |
          node --check app.js
          node -e "global.window={};require('./data/content.js');console.log(window.PATIENT_CARE_CONTENT.meta.contentVersion)"
          node -e "JSON.parse(require('fs').readFileSync('data/schema.json','utf8'));console.log('schema ok')"
          git diff --check
          grep -q "shineCompleted:false" app.js
          grep -q "shineCompleted=true" app.js
          grep -q "profile().guided.shineCompleted!==true" app.js
      - run: |
          rm -f .github/fix-shine-first-entry.py .github/workflows/fix-shine-first-entry.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Gate SHINE behind priority wizard"
          git push
