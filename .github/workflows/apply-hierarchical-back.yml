# IF YOU HAVE NOT ALREADY READ PATIENT_CARE_RULES.md, STOP AND READ IT BEFORE EDITING THIS FILE.
name: Apply hierarchical back navigation
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-hierarchical-back.yml
      - .github/apply-hierarchical-back.py
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - run: python3 .github/apply-hierarchical-back.py
      - run: |
          node --check app.js
          node -e "global.window={};require('./data/content.js');console.log(window.PATIENT_CARE_CONTENT.meta.contentVersion)"
          node -e "JSON.parse(require('fs').readFileSync('data/schema.json','utf8'));console.log('schema ok')"
          git diff --check
          grep -q "function renderBackControl" app.js
          grep -q "return renderBackControl(`#/${area}/${sectionId}`" app.js
          grep -q "renderBackControl('#/heal','HEAL home','heal')" app.js
      - run: |
          rm -f .github/apply-hierarchical-back.py .github/workflows/apply-hierarchical-back.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Apply hierarchical back navigation"
          git push
